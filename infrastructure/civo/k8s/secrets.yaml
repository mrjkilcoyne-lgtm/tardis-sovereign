# IMPORTANT: This is a TEMPLATE. Replace placeholder values before applying.
# For production, use sealed-secrets or external-secrets-operator instead.
#
# To create from CLI instead of applying this file:
#   kubectl create secret generic sovereign-dispatch-secrets \
#     --namespace=tardis-sovereign \
#     --from-literal=civo-api-key=YOUR_CIVO_API_KEY \
#     --from-literal=api-secret-key=YOUR_SECRET_KEY
#
apiVersion: v1
kind: Secret
metadata:
  name: sovereign-dispatch-secrets
  namespace: tardis-sovereign
  labels:
    app.kubernetes.io/name: sovereign-dispatch
    app.kubernetes.io/part-of: tardis-sovereign
type: Opaque
stringData:
  # REPLACE THESE BEFORE APPLYING
  civo-api-key: "PLACEHOLDER_REPLACE_WITH_CIVO_API_KEY"
  api-secret-key: "PLACEHOLDER_REPLACE_WITH_RANDOM_SECRET"
