# PLACEHOLDER Gitea admin secret.
# Phase 4 replaces this with a SOPS-decrypted value committed as
# workloads/secrets/tardis-secrets.enc.yaml. Do NOT apply this file
# verbatim with the placeholder below — rotate before first Gitea boot.
apiVersion: v1
kind: Secret
metadata:
  name: gitea-admin
  namespace: tardis
  labels:
    app: gitea
    app.kubernetes.io/part-of: tardis
type: Opaque
stringData:
  admin-password: "PLACEHOLDER_GITEA_ADMIN_PASSWORD"
